% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_plot.R
\name{plot_anchor_candidates}
\alias{plot_anchor_candidates}
\title{Plot anchor candidate information criteria after item screening}
\usage{
plot_anchor_candidates(anchor_df, criterion = NULL, n_anchors = 2L)
}
\arguments{
\item{anchor_df}{Data frame as returned by \code{\link[=summarise_anchor_candidates]{summarise_anchor_candidates()}}.}

\item{criterion}{Character.  Information criterion to plot.  Defaults to
the \code{"criterion"} attribute of \code{anchor_df}, or \code{"SABIC"} if absent.}

\item{n_anchors}{Integer.  Number of anchor items per facet to highlight.
Should match the \code{N_ANCHORS} constant used in the pipeline.  Default 2.}
}
\value{
In ggplot2 mode: the \code{ggplot} object, invisibly.
In base-R mode: \code{NULL} invisibly.  Plots are drawn as side-effects in
both cases.
}
\description{
Produces a dot/lollipop plot of Δ-AIC, Δ-BIC, or Δ-SABIC per item for
each facet.  Items are coloured by DIF type and the proposed anchor items
(top \code{n_anchors} clean candidates per facet) are highlighted.  A dashed
vertical line at 0 separates items that improve fit when freed (left) from
those that do not (right).
}
\details{
The plot is printed immediately so it appears in RStudio / a graphics
device without an explicit \code{print()} call.
}
\seealso{
\code{\link[=summarise_anchor_candidates]{summarise_anchor_candidates()}}
}
