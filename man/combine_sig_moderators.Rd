% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/significance.R
\name{combine_sig_moderators}
\alias{combine_sig_moderators}
\title{Combine screening-step significance results into a single data frame}
\usage{
combine_sig_moderators(
  sig.means = NULL,
  sig.variances = NULL,
  item.mods = NULL
)
}
\arguments{
\item{sig.means}{Named list of latent mean significance results, one element
per facet.  Each element is the direct output of
\code{\link[=significant_mean_moderators]{significant_mean_moderators()}} (a list with \verb{$table} and \verb{$summary}).
List names are the facet names (e.g. \code{list(P_1 = ..., P_2 = ...)}).}

\item{sig.variances}{Named list of latent variance significance results,
same structure as \code{sig.means}, from \code{\link[=significant_var_moderators]{significant_var_moderators()}}.}

\item{item.mods}{Named list of item-level significance results.  Keys must
use the composite \code{"facet_name.item_name"} format
(e.g. \code{"P_1.p1_i5"}).  Each value is the output of
\code{\link[=significant_item_moderators]{significant_item_moderators()}}.  In the pipeline this list is produced by
\code{unlist(sig_items, recursive = FALSE)} after the per-facet screening loop.}
}
\value{
A data frame with columns: \code{facet}, \code{item}, \code{type}, \code{moderator},
\code{Estimate}, \code{Std.Error}, \code{z}, \code{p}, \code{p_corrected}, \code{significant},
\code{significant_corrected}, \code{param_name}, \code{source}.  The \code{type} column takes
values \code{"mean"}, \code{"variance"}, \code{"loading"}, or \code{"intercept"}.
}
\description{
Merges the outputs of \code{\link[=significant_mean_moderators]{significant_mean_moderators()}},
\code{\link[=significant_var_moderators]{significant_var_moderators()}}, and per-item calls to
\code{\link[=significant_item_moderators]{significant_item_moderators()}} into a standardised data frame that can
be passed to \code{\link[=build_full_domain_models]{build_full_domain_models()}}.
}
\seealso{
\code{\link[=build_full_domain_models]{build_full_domain_models()}}, \code{\link[=significant_item_moderators]{significant_item_moderators()}}
}
