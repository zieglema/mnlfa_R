% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/individual_params.R
\name{build_individual_intercepts}
\alias{build_individual_intercepts}
\title{Compute person-specific item intercepts}
\usage{
build_individual_intercepts(
  param_df,
  data,
  significance_level = NULL,
  p_adjust_method = NULL,
  verbose = FALSE
)
}
\arguments{
\item{param_df}{Data frame returned in the \verb{$param_df} slot of a
\code{\link[=run_mxsem]{run_mxsem()}} result.}

\item{data}{Data frame with the original observations and moderator columns.}

\item{significance_level}{Optional numeric. If supplied, only moderators
with \code{p <= significance_level} are included.}

\item{p_adjust_method}{Optional character. P-value adjustment method passed
to \code{\link[stats:p.adjust]{stats::p.adjust()}} before applying \code{significance_level}.}

\item{verbose}{Logical. If \code{TRUE}, additional diagnostic columns are added.
Default is \code{FALSE}.}
}
\value{
A data frame with \code{nrow(data)} rows and one prediction column per
item (named \verb{<item>_pred}) plus intercept columns (\verb{<item>_intercept}).
}
\description{
For each item with an intercept moderation in \code{param_df}, calculates the
individual-level intercept as:
\code{intercept_base + sum(beta_k * moderator_k)}.
}
\seealso{
\code{\link[=build_all_individual_parameters]{build_all_individual_parameters()}}
}
