% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_plot.R
\name{extract_moderation_table}
\alias{extract_moderation_table}
\title{Extract a summary table of all moderation effects}
\usage{
extract_moderation_table(
  param_df,
  moderators = NULL,
  alpha = 0.05,
  correction_method = "none"
)
}
\arguments{
\item{param_df}{Data frame from the \verb{$param_df} slot of \code{\link[=run_mxsem]{run_mxsem()}}.}

\item{moderators}{Optional character vector of moderator names in the order
used during model fitting.  Recovered from \code{attr(param_df, "moderators")}
if \code{NULL}.}

\item{alpha}{Significance level.  Default \code{0.05}.}

\item{correction_method}{P-value correction method passed to
\code{\link[stats:p.adjust]{stats::p.adjust()}}.  Default \code{"none"}.}
}
\value{
A data frame with columns:
\describe{
\item{\code{param_type}}{One of \code{"latent_mean"}, \code{"latent_variance"},
\code{"loading"}, \code{"intercept"}, \code{"covariance"}.}
\item{\code{facet}}{Latent variable name (e.g. \code{"P_1"}).}
\item{\code{item}}{Item name for \code{"loading"} / \code{"intercept"} rows; \code{NA}
otherwise.}
\item{\code{moderator}}{Name of the moderator variable.}
\item{\code{Estimate}}{Regression coefficient (unstandardised).}
\item{\code{Std.Error}}{Standard error.}
\item{\code{z}}{z-statistic.}
\item{\code{p}}{Two-sided p-value.}
\item{\code{p_corrected}}{Corrected p-value (via \code{correction_method}).}
\item{\code{significant}}{Logical: \code{p_corrected <= alpha}.}
}
}
\description{
Collects every moderation-slope parameter from \code{param_df} (the \verb{$param_df}
slot of a \code{\link[=run_mxsem]{run_mxsem()}} result) into a single, human-readable table.  Each
row represents the partial effect of one moderator on one target parameter
(loading, intercept, latent mean, latent variance, or covariance).
}
\details{
Baseline parameters (intercepts of the formulas, e.g. \code{l_P_1_0_3},
\code{v0_P_1}, \code{m_P_1}) are excluded; only the slope coefficients are returned.
}
\seealso{
\code{\link[=compute_conditional_parameters]{compute_conditional_parameters()}}, \code{\link[=run_mxsem]{run_mxsem()}}
}
