% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/domain_models.R
\name{create_acquiescence_lines}
\alias{create_acquiescence_lines}
\title{Generate acquiescence bifactor syntax lines for a domain}
\usage{
create_acquiescence_lines(facet_models, moderators = NULL)
}
\arguments{
\item{facet_models}{Named list of per-facet model strings (same as the
first argument of \code{\link[=build_full_domain_models]{build_full_domain_models()}}).  Item names are parsed
from the \verb{=~} measurement lines.}

\item{moderators}{Optional character vector.  If supplied, acquiescence
mean regressions (\code{acq ~ m_acq_k * mod_k}) are added for each moderator.}
}
\value{
A named list (one element per domain) of character vectors with
acquiescence factor syntax lines.
}
\description{
Creates the syntax for an orthogonal acquiescence factor (\code{acq}) with fixed
loadings of \code{+1} (regular items) or \code{-1} (reversed items, identified by
the substring \verb{_rec_} in the item name).  Optionally adds latent mean
regressions on the acquiescence factor.
}
\details{
The returned list is intended to be passed as \code{extra_lines_per_domain} to
\code{\link[=build_full_domain_models]{build_full_domain_models()}}.

\strong{Reversed-item convention.}  An item is treated as reversed if and only if
its name contains the exact substring \verb{_rec_} (case-sensitive).  This must
be present in both the data column name and the model syntax string.
Example: rename \code{e1_i8} → \code{e1_rec_i8} in your data frame and use the same
name in the measurement line.  Items without \verb{_rec_} receive a \code{+1} loading
on the acquiescence factor.

The acquiescence factor is constrained to be orthogonal to all content
factors (\code{acq ~~ 0 * LV}) and its mean is fixed to zero (\code{acq ~ 0*1})
because acquiescence is assumed to be independent of and unconfounded with
the content factors.
}
\seealso{
\code{\link[=build_full_domain_models]{build_full_domain_models()}}
}
