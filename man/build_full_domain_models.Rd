% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/domain_models.R
\name{build_full_domain_models}
\alias{build_full_domain_models}
\title{Build full domain models with all significant moderations}
\usage{
build_full_domain_models(
  facet_models,
  all_sig_mods,
  anchor_items,
  moderators = NULL,
  extra_lines_per_domain = NULL
)
}
\arguments{
\item{facet_models}{Named list of per-facet baseline model strings.  Names
must follow the convention \code{"[LETTER]_[DIGIT(S)]"} (e.g. \code{"E_1"},
\code{"P_3"}).  All facets with the same letter prefix (e.g. \code{P_1}, \code{P_2},
\code{P_3}) are assembled into a single domain model.}

\item{all_sig_mods}{Data frame from \code{\link[=combine_sig_moderators]{combine_sig_moderators()}}.}

\item{anchor_items}{Character vector of anchor identifiers in the form
\code{"facet_name.item_name"}.}

\item{moderators}{Optional character vector of all moderator names (used to
compute correct numeric parameter indices).}

\item{extra_lines_per_domain}{Optional named list (one element per domain)
of additional syntax lines appended after the facet models and covariance
lines.  Typically the output of \code{\link[=create_acquiescence_lines]{create_acquiescence_lines()}}.}
}
\value{
A named list of character strings, one per domain.
}
\description{
For each domain (defined as the prefix before the first \verb{_} in the facet
name), this function:
\enumerate{
\item Calls \code{\link[=add_all_moderation_single_facet]{add_all_moderation_single_facet()}} for each facet.
\item Adds labelled covariance lines between all pairs of facets within
the domain.
\item Optionally appends extra syntax lines (e.g. acquiescence factor
lines from \code{\link[=create_acquiescence_lines]{create_acquiescence_lines()}}).
}
}
\details{
The result is a complete domain model string ready for \code{\link[=run_mxsem]{run_mxsem()}} with
\code{scale_loadings = FALSE} and \code{scale_latent_variances = FALSE}.  Scale
identification is achieved by fixing the first anchor item's loading to 1
(handled automatically by \code{\link[=add_all_moderation_single_facet]{add_all_moderation_single_facet()}}).

\strong{Important}: if a facet has \emph{no} significant moderations in \code{all_sig_mods},
\code{\link[=add_all_moderation_single_facet]{add_all_moderation_single_facet()}} returns the original \code{model_string}
unchanged (including its \code{LV ~~ 1*LV} baseline variance constraint).  This
situation arises only when all moderation tests are non-significant and is
typically not a problem in practice; however users should verify anchor
selection is still applied in such cases.
}
\seealso{
\code{\link[=add_all_moderation_single_facet]{add_all_moderation_single_facet()}}, \code{\link[=run_mxsem]{run_mxsem()}},
\code{\link[=create_acquiescence_lines]{create_acquiescence_lines()}}
}
