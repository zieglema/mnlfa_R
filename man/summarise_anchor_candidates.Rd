% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_plot.R
\name{summarise_anchor_candidates}
\alias{summarise_anchor_candidates}
\title{Summarise item-screening fit indices for anchor selection}
\usage{
summarise_anchor_candidates(
  item_fit_results,
  baseline_fits,
  sig_items = NULL,
  criterion = c("SABIC", "AIC", "BIC")
)
}
\arguments{
\item{item_fit_results}{Named list (level 1: facets; level 2: items) of
\code{fit_df} data frames as returned by \code{run_mxsem()$fit_df}.}

\item{baseline_fits}{Named list (one element per facet) of \code{run_mxsem()}
results for the baseline CFAs.  The \verb{$fit_df} component is used.}

\item{sig_items}{Optional nested list (level 1: facets; level 2: items) of
\code{significant_item_moderators()} results.  When supplied, each item is
annotated with its DIF type.}

\item{criterion}{Character.  The information criterion used for sorting
within facets.  One of \code{"SABIC"} (default), \code{"AIC"}, or \code{"BIC"}.}
}
\value{
A data frame with columns \code{facet}, \code{item}, \code{delta_AIC},
\code{delta_BIC}, \code{delta_SABIC}, \code{dif_type} (character: \code{"none"},
\code{"intercept"}, \code{"loading"}, or \code{"loading + intercept"}).  Rows are
sorted descending by the chosen criterion within each facet (highest
Δ-IC first = best anchor candidate).  The chosen \code{criterion} is stored
as an attribute.
}
\description{
Computes Δ-AIC, Δ-BIC, and Δ-SABIC for every item relative to the
corresponding facet's baseline CFA, and annotates each item with its DIF
type (none / intercept / loading / loading + intercept) based on the
significance results from the screening step.
}
\details{
The returned data frame is intended to be inspected visually (via
\code{\link[=plot_anchor_candidates]{plot_anchor_candidates()}}) before the user decides how many items to use
as anchors (\code{N_ANCHORS}) and which information criterion to rank by
(\code{ANCHOR_CRITERION}).
}
\seealso{
\code{\link[=plot_anchor_candidates]{plot_anchor_candidates()}}, \code{\link[=moderate_loadings_and_intercepts]{moderate_loadings_and_intercepts()}},
\code{\link[=significant_item_moderators]{significant_item_moderators()}}
}
