% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_building.R
\name{moderate_loadings_and_intercepts}
\alias{moderate_loadings_and_intercepts}
\title{Build per-item screening models (moderated loadings and intercepts)}
\usage{
moderate_loadings_and_intercepts(
  model_string,
  moderators,
  scale_loadings = FALSE
)
}
\arguments{
\item{model_string}{Character. Baseline model syntax (one latent variable).}

\item{moderators}{Character vector of moderator names.  The names must
match column names in the data frame that will be passed to \code{\link[=run_mxsem]{run_mxsem()}}.}

\item{scale_loadings}{Logical.  If \code{FALSE} (default) the latent variance is
fixed to 1 in every item model (variance-based identification).  If
\code{TRUE} identification is via the first loading; a marker-swap is applied
automatically when the first item is the DIF target.}
}
\value{
A named list of character strings, one per item in the measurement
model.  List names are the item names as they appear in \code{model_string}.
When \code{scale_loadings = TRUE}, fit each returned model with
\code{run_mxsem(..., scale_loadings = TRUE, scale_latent_variances = FALSE)};
when \code{scale_loadings = FALSE}, use
\code{run_mxsem(..., scale_loadings = FALSE, scale_latent_variances = TRUE)}.
}
\description{
For each item in the measurement model, creates a separate model string in
which that item's loading and intercept are each expressed as a linear
function of the supplied moderators.
}
\details{
\subsection{Identification strategies}{

Two identification strategies are supported via \code{scale_loadings}:
\itemize{
\item \strong{\code{scale_loadings = FALSE} (default):} The latent variance is fixed to 1
(\code{LV ~~ 1*LV}) in every item model.  This is the recommended default
because it allows every item — including the first — to have a freely
moderated loading without any identification problem.  The returned models
should be passed to \code{\link[=run_mxsem]{run_mxsem()}} with \code{scale_loadings = FALSE} and
\code{scale_latent_variances = TRUE} (or the variance constraint already
present in the model string is sufficient).
\item \strong{\code{scale_loadings = TRUE}:} The latent variance is free; scale
identification relies on fixing the \strong{first loading in the \verb{=~} line}
to 1 (handled automatically by \code{\link[=run_mxsem]{run_mxsem()}} via \code{scale_loadings = TRUE}).
\strong{Special case — marker item:} when the item being tested for DIF \emph{is}
the first item (the current marker), fixing its loading to 1 and
simultaneously allowing it to vary with the moderators is a
contradiction.  The function therefore \strong{swaps items 1 and 2} in the
\verb{=~} line for that specific model so that \code{\link[=run_mxsem]{run_mxsem()}} fixes item 2's
loading instead, leaving item 1's loading free for the DIF test.  The
returned models should all be passed to \code{\link[=run_mxsem]{run_mxsem()}} with
\code{scale_loadings = TRUE} and \code{scale_latent_variances = FALSE}.
}

\strong{Parameter naming convention} used internally:
\itemize{
\item \verb{[facet]_load_[ii]} — definition-variable label for the target
item's loading (zero-padded two-digit item index \code{ii}).
\item \verb{l_[facet]_0_[i]} — baseline (intercept) of the loading formula.
\item \verb{l_[facet]_[i]_[k]} — slope of the loading on moderator \code{k}.
\item \verb{int_[facet]_[item]} — baseline intercept of the target item.
\item \verb{int_[facet]_[item]_[kk]} — slope of the item intercept on
moderator \code{k} (zero-padded two-digit moderator index \code{kk}).
}
}
}
\seealso{
\code{\link[=significant_item_moderators]{significant_item_moderators()}}, \code{\link[=run_mxsem]{run_mxsem()}}
}
