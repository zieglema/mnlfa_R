% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_plot.R
\name{compute_conditional_parameters}
\alias{compute_conditional_parameters}
\title{Compute parameter values conditional on a moderator grid}
\usage{
compute_conditional_parameters(
  param_df,
  moderator_grid,
  moderators = NULL,
  param_types = c("latent_mean", "latent_variance", "loading", "intercept")
)
}
\arguments{
\item{param_df}{Data frame from the \verb{$param_df} slot of \code{\link[=run_mxsem]{run_mxsem()}}.}

\item{moderator_grid}{A \code{data.frame} whose columns are the moderator
variables.  Typically produced with \code{\link[base:expand.grid]{base::expand.grid()}}, e.g.
\code{expand.grid(age_z = seq(-2, 2, 0.5), sex = c(0, 1))}.}

\item{moderators}{Optional character vector.  Recovered from
\code{attr(param_df, "moderators")} if \code{NULL}.}

\item{param_types}{Character vector restricting which parameter types are
included.  Any subset of
\code{c("latent_mean", "latent_variance", "loading", "intercept")}.
Default: all four.}
}
\value{
A long-format \code{data.frame} combining all columns of
\code{moderator_grid} with:
\describe{
\item{\code{parameter}}{Unique label, e.g. \code{"P_1_mean"}, \code{"p1_i5_intercept"},
\code{"p3_i7_loading"}.}
\item{\code{param_type}}{Parameter type.}
\item{\code{facet}}{Latent variable name.}
\item{\code{item}}{Item name or \code{NA}.}
\item{\code{value}}{Conditional parameter value at the given moderator values.}
}
}
\description{
For each moderated parameter in \code{param_df}, evaluates the parameter value
at every row of \code{moderator_grid}.  This produces a long-format data frame
that is directly suitable for plotting conditional (DIF) curves.
}
\details{
\strong{Interaction plots.} Passing an \code{expand.grid()} of a continuous and a
binary moderator (e.g. \code{expand.grid(age_z = seq(-2, 2, .5), sex = c(0, 1))})
produces separate trajectories per binary level, which can then be rendered
as an interaction graph via \code{\link[=plot_conditional_parameters]{plot_conditional_parameters()}} or \code{ggplot2}.

\strong{Formula per parameter type:}
\itemize{
\item Latent mean: \eqn{m_0 + \sum_k m_k \cdot \text{mod}_k}
\item Loading:     \eqn{l_0 + \sum_k l_k \cdot \text{mod}_k}
\item Intercept:   \eqn{\tau_0 + \sum_k \tau_k \cdot \text{mod}_k}
\item Variance:    \eqn{\exp(v_0 + \sum_k v_k \cdot \text{mod}_k)}
}
}
\examples{
\dontrun{
grid <- expand.grid(age_z = seq(-2, 2, 0.5), sex = c(0, 1))
cond <- compute_conditional_parameters(fit_domain$param_df, grid)

# Interaction plot for the loading of p3_i7
plot_conditional_parameters(cond, x_var = "age_z", group_var = "sex",
                             param_types = "loading")
}

}
\seealso{
\code{\link[=extract_moderation_table]{extract_moderation_table()}}, \code{\link[=plot_conditional_parameters]{plot_conditional_parameters()}}
}
