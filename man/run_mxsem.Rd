% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_model.R
\name{run_mxsem}
\alias{run_mxsem}
\title{Fit a moderated SEM via mxsem / OpenMx}
\usage{
run_mxsem(
  model_string,
  data,
  extract_fun = extract_mx_info,
  scale_loadings = TRUE,
  scale_latent_variances = FALSE,
  start_values = NULL,
  add_ref_models = TRUE,
  moderators = NULL,
  seed = NULL,
  extraTries = 10
)
}
\arguments{
\item{model_string}{A character string containing the lavaan-style model
syntax (single string with \verb{\\n} separators, or a character vector of
lines).}

\item{data}{A \code{data.frame} (or coercible object) containing the observed
variables and moderators.}

\item{extract_fun}{Function used to extract fit statistics and parameters
from the OpenMx summary.  Defaults to \code{\link[=extract_mx_info]{extract_mx_info()}}.}

\item{scale_loadings}{Logical. If \code{TRUE} (default), the first factor
loading is fixed to 1 for identification.  Set to \code{FALSE} when
the loading constraint is encoded in \code{model_string} (e.g. \code{1*item}).}

\item{scale_latent_variances}{Logical. If \code{TRUE}, the latent variance is
fixed to 1.  Use \code{TRUE} for screening-step models, \code{FALSE} for full
domain models.  Default is \code{FALSE}.}

\item{start_values}{Optional named numeric vector of starting values passed
to \code{\link[mxsem:set_starting_values]{mxsem::set_starting_values()}}.}

\item{add_ref_models}{Logical. Whether to compute reference models needed
for CFI/TLI/RMSEA.  Default is \code{TRUE}.  \strong{For all MNLFA pipeline steps,
set this to \code{FALSE}}: MNLFA models use definition variables (person-
specific parameters), which makes the standard independence reference model
inapplicable.  The SABIC (returned in \code{fit_df$SABIC}) is used for all
model comparisons instead.  Setting \code{add_ref_models = FALSE} also
substantially speeds up model fitting.}

\item{moderators}{Optional character vector of moderator variable names.
When supplied, the names are stored as an attribute on \code{param_df} so that
downstream significance functions can recover the original moderator
labels from numeric indices in parameter names.  The order must match the
numeric suffixes used in parameter labels (e.g. \code{m_E_1_01} corresponds to
\code{moderators[1]}).}

\item{seed}{Optional integer seed for reproducibility.}

\item{extraTries}{Number of additional optimisation attempts passed to
\code{\link[OpenMx:mxTryHard]{OpenMx::mxTryHard()}}.  Default is \code{10}.}
}
\value{
A named list with the following components:
\describe{
\item{\code{model_string}}{The model syntax as supplied.}
\item{\code{script}}{The OpenMx model object (before fitting).}
\item{\code{fit}}{The fitted OpenMx model object.}
\item{\code{ref_models}}{Reference model list (or \code{NULL} if
\code{add_ref_models = FALSE}).}
\item{\code{summary}}{The OpenMx model summary.}
\item{\code{extracted}}{Raw output of \code{extract_fun}.}
\item{\code{fit_df}}{Data frame of fit statistics.}
\item{\code{param_df}}{Data frame of parameter estimates, standard errors,
z-values, and p-values, enriched with columns \code{type}, \code{facet}, and
\code{domain}.}
}
}
\description{
Converts a lavaan-style model string to an OpenMx model object using
\code{\link[mxsem:mxsem]{mxsem::mxsem()}}, fits it with \code{\link[OpenMx:mxTryHard]{OpenMx::mxTryHard()}}, and returns a
richly annotated list that is consumed by the downstream significance- and
parameter-extraction functions.
}
\seealso{
\code{\link[=extract_mx_info]{extract_mx_info()}}, \code{\link[=significant_mean_moderators]{significant_mean_moderators()}},
\code{\link[=significant_item_moderators]{significant_item_moderators()}}
}
