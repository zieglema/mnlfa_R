% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_plot.R
\name{plot_conditional_parameters}
\alias{plot_conditional_parameters}
\title{Plot conditional parameter values across a moderator grid}
\usage{
plot_conditional_parameters(
  cond_params,
  x_var,
  group_var = NULL,
  param_types = NULL,
  xlab = x_var,
  ylab = "Parameter value",
  main_prefix = "",
  ncol = 3L,
  ...
)
}
\arguments{
\item{cond_params}{Data frame returned by \code{\link[=compute_conditional_parameters]{compute_conditional_parameters()}}.}

\item{x_var}{Character. Column in \code{cond_params} to use as x-axis (typically
a continuous moderator, e.g. \code{"age_z"}).}

\item{group_var}{Optional character. Column to use for separate lines
(e.g. \code{"sex"}).  \code{NULL} draws a single line per parameter.}

\item{param_types}{Optional character vector to restrict which parameter
types are plotted.  Defaults to all types present in \code{cond_params}.}

\item{xlab}{Character. X-axis label.  Defaults to \code{x_var}.}

\item{ylab}{Character. Y-axis label.  Default \code{"Parameter value"}.}

\item{main_prefix}{Character. Prefix prepended to each panel title.
Default \code{""}.}

\item{ncol}{Integer. Number of plot columns in the base-R layout.
Default \code{3}.}

\item{...}{Further arguments passed to \code{plot()} (base-R mode only).}
}
\value{
In ggplot2 mode: a named list of \code{ggplot} objects (one per
parameter), returned invisibly.  Each plot is also printed immediately
so it appears in RStudio / a graphics window without an explicit
\code{print()} call.  In base-R mode: \code{NULL} invisibly; plots are drawn as
a side-effect.
}
\description{
Renders the output of \code{\link[=compute_conditional_parameters]{compute_conditional_parameters()}} as line plots —
one panel per moderated parameter.  If \strong{ggplot2} is installed, a list of
\code{ggplot} objects is returned; otherwise base-R graphics are drawn.
}
\details{
\strong{Interaction / Zweifachinteraktions-plots.}  Specify \code{x_var} as the
continuous moderator and \code{group_var} as the binary (or categorical) one.
Each level of \code{group_var} is drawn as a separate line, producing a
classic two-way interaction graph.
}
\seealso{
\code{\link[=compute_conditional_parameters]{compute_conditional_parameters()}}, \code{\link[=extract_moderation_table]{extract_moderation_table()}}
}
