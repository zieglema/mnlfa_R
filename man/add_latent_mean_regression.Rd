% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_building.R
\name{add_latent_mean_regression}
\alias{add_latent_mean_regression}
\title{Add latent mean intercept and moderator regressions to model syntax}
\usage{
add_latent_mean_regression(
  model_string,
  moderators,
  identification = c("variance", "loading")
)
}
\arguments{
\item{model_string}{Character. Lavaan-style model syntax (single string or
character vector of lines).}

\item{moderators}{Character vector of moderator variable names.}

\item{identification}{Character. Scale identification strategy used when
fitting the resulting model with \code{\link[=run_mxsem]{run_mxsem()}}.  \code{"variance"} (default,
Bauer 2017) keeps the \code{LV ~~ 1*LV} constraint so the latent variance
is fixed to 1; pass \code{scale_latent_variances = TRUE} to \code{\link[=run_mxsem]{run_mxsem()}}.
\code{"loading"} removes the variance constraint so that scale identification
is via the first loading being fixed to 1; pass
\verb{scale_loadings = TRUE, scale_latent_variances = FALSE} to \code{\link[=run_mxsem]{run_mxsem()}}.}
}
\value{
A single character string (lines joined by \verb{\\n}).
}
\description{
Appends a labelled intercept line (\code{latent ~ m_latent*1}) and one or more
moderator regression lines (\code{latent ~ m_latent_01*mod1 + ...}) to the
model string.  Any pre-existing mean/intercept lines for the latent variable
are removed first to avoid duplication.
}
\examples{
m <- "F =~ x1 + x2 + x3\nF ~~ 1*F\nF ~ 0*1"
add_latent_mean_regression(m, c("age", "sex"))
add_latent_mean_regression(m, c("age", "sex"), identification = "loading")

}
